name = "push"
description = "push changes to the git repository"
version = "0.1.0"
author = "primiti_ve"

[workflow.env]
shell = "pwsh"

[workflow.arguments.message]
name = "title"
type = "positional"
required = true

[workflow.arguments.description]
name = "description"
type = "positional"
default = ""
required = false

[workflow.steps.add]
command = "git"
arguments = ["add", "-A"]
order = 1

[workflow.steps.commit]
command = "git"
arguments = {
    if description != "" {
        ["commit", "-m", "{{title}}", "-m", "{{description}}"]
    } else {
        ["commit", "-m", "{{title}}"]
    }
}
order = 2

[workflow.steps.push]
command = "git"
arguments = ["push", "origin", "main"]
order = 3
