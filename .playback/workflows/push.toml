name = "push"
description = "push changes to the git repository"
version = "0.1.0"
author = "primiti_ve"

[workflow.env]
shell = "pwsh"

[workflow.steps."push commits"]
command = "git"
arguments = ["push", "origin", "main"]
order = 1
